---
alwaysApply: true
description: Homepage agent guidelines — extends shared server guidelines
---

# AI Agent Guidelines for Homepage

**Shared server guidelines** (iptables fix, container inventory, Tailscale IP, forbidden ops):
→ `/home/brandon/projects/.augment-guidelines`

---

## 🚨 Critical Rules (Always Apply)

- **NEVER**: `sudo reboot`, `shutdown`, `docker-compose down`, `DROP DATABASE`, `pg_resetwal`
- **Tailscale IP**: `100.69.184.113`
- **Container**: see docker-compose.yml
- **After any redeploy**, if externally unreachable: check for stale iptables DNAT rules:
  `sudo iptables-save | grep DNAT` — remove any duplicate entries for the service port

## Restarting Services

`docker restart` / `docker-compose restart` FAIL on this server. Use:
```bash
PID=$(docker inspect <container_name> --format '{{.State.Pid}}')
kill $PID
cd /home/brandon/projects/Homepage
docker-compose up -d
```

## Project Details

- **Directory**: `/home/brandon/projects/Homepage/`
- **Port**: varies
- **URL**: varies
